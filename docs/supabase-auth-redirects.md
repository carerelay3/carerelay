# Supabase Auth Redirects

Add this exact URL to Supabase Auth URL Configuration before password recovery is used in production:

```text
https://carerelay.xyz/reset-password
```

For local testing, also allow:

```text
http://localhost:3000/reset-password
```
